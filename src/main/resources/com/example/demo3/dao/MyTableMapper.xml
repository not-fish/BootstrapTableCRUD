<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >

<mapper namespace="com.example.demo3.dao.MyTableMapper">

    <select id="getTableById" resultType="com.example.demo3.entity.MyTable">
        SELECT * FROM mytable02 WHERE id = #{id}
    </select>

    <select id="getAll" resultType="com.example.demo3.entity.MyTable">
            SELECT * FROM mytable02
    </select>

    <update id="updateTable" parameterType="com.example.demo3.entity.MyTable">
            UPDATE mytable02
            <trim prefix="set" suffixOverrides=",">
                <if test="name != null and name != ''">name=#{name},</if>
                <if test="phone != null and phone != ''">phone=#{phone},</if>
                <if test="userImg != null and userImg != ''">user_img=#{userImg},</if>
            </trim>
            WHERE id = #{id}
    </update>

    <delete id="deleteTable" parameterType="com.example.demo3.entity.MyTable">
            DELETE FROM mytable02
            WHERE id = #{id}
    </delete>

    <insert id="addTable" parameterType="com.example.demo3.entity.MyTable">
            INSERT INTO mytable02
            <trim prefix="(" suffix=')' suffixOverrides=",">
                <if test="id != null and id != ''">id,</if>
                <if test="name != null and name != ''">name,</if>
                <if test="phone != null and phone != ''">phone,</if>
                <if test="userImg != null and userImg != ''">user_img,</if>
            </trim>

            <trim prefix="values (" suffix=")" suffixOverrides=",">
                <if test="id != null and id != ''">#{id},</if>
                <if test="name != null and name != ''">#{name},</if>
                <if test="phone != null and phone != ''">#{phone},</if>
                <if test="userImg != null and userImg != ''">#{userImg},</if>
            </trim>
    </insert>

    <select id="queryTable" parameterType="com.example.demo3.entity.MyTable" resultType="com.example.demo3.entity.MyTable">
            SELECT * FROM mytable02
            WHERE
            <trim >
                <if test="id != null and id != ''">id=</if>
                <if test="name != null and name != ''">name=</if>
                <if test="phone != null and phone != ''">phone=</if>
            </trim>

            <trim>
                <if test="id != null and id != ''">#{id}</if>
                <if test="name != null and name != ''">#{name}</if>
                <if test="phone != null and phone != ''">#{phone}</if>
            </trim>
    </select>

</mapper>