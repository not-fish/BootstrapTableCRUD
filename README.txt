[TODO]
1、完成用户认证与授权

[BUG]
1、文件上传的问题，如果是小于7~8k的图片，就无法下载（下载下来只有1k）
[fix] 把下载的方法的返回类型改为 void
2、页面点击左侧菜单栏的按钮后，整体页面网上挪动
[fix] 没找出具体原因，不过已经通过浏览器调节 margin-top 参数解决高度变动问题
3、新页面header.html的模糊效果会因为转换页面而失效，初步判断为js冲突
[fix] js中全局变量不会自动销毁，所以每次加载js时会出现变量重复（造成冲突），把要运行的代码封装成函数，在全局调用即可
虽然还是重复加载了js，但是监听器会自动销毁，而定时任务启动之后就无法关闭，会造成重叠，所以把定时任务丢到总的js中，让它只在开始加载一次
